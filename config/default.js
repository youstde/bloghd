const config = {
  // 启动端口
  port: 7001,
  // 数据库配置
  database: {
    DATABASE: 'blogone',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config;